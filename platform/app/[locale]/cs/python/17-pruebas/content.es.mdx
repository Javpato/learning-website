# Pruebas (testing)

<Accroche>« Código sin pruebas es código que crees que funciona. Con pruebas, lo sabes. »</Accroche>

Escribir **pruebas** es comprobar automáticamente que tu código hace lo que esperas. Te da confianza
para cambiar cosas sin miedo: si algo se rompe, una prueba te avisa al instante.

## `assert` — la prueba más simple

`assert condicion` no hace nada si la condición es verdadera, y **lanza un error** si es falsa. Es la
base de todas las pruebas:

```python
def doble(x):
    return x * 2

assert doble(3) == 6
assert doble(0) == 0
print("Todas las pruebas pasaron")
```

Si alguna afirmación fallara, el programa se detendría con un `AssertionError`.

{/* sol:
def doble(x):
    return x * 2

assert doble(3) == 6
assert doble(0) == 0
print("Todas las pruebas pasaron")
*/}
<PyExercise
  task="Comprueba con assert que doble(3) es 6 y doble(0) es 0; luego imprime «Todas las pruebas pasaron»."
  starter={`def doble(x):
    return x * 2

# escribe dos assert
print("Todas las pruebas pasaron")`}
  expected_output="Todas las pruebas pasaron"
  hint={`assert doble(3) == 6 y assert doble(0) == 0. Si pasan, se llega al print.`}
/>

## Una tabla de casos

Un patrón muy práctico: una lista de `(entrada, resultado_esperado)` y un bucle que las comprueba:

```python
def es_par(n):
    return n % 2 == 0

casos = [(2, True), (3, False), (0, True)]
for entrada, esperado in casos:
    assert es_par(entrada) == esperado
print(f"{len(casos)} pruebas OK")
```

{/* sol:
def es_par(n):
    return n % 2 == 0

casos = [(2, True), (3, False), (0, True)]
for entrada, esperado in casos:
    assert es_par(entrada) == esperado
print(f"{len(casos)} pruebas OK")
*/}
<PyExercise
  task="Prueba es_par con una tabla de casos y, si todos pasan, imprime «3 pruebas OK»."
  starter={`def es_par(n):
    return n % 2 == 0

casos = [(2, True), (3, False), (0, True)]
for entrada, esperado in casos:
    assert
print(f"{len(casos)} pruebas OK")`}
  expected_output="3 pruebas OK"
  hint={`assert es_par(entrada) == esperado dentro del bucle.`}
/>

## `unittest` — el marco de pruebas estándar

Para proyectos de verdad, Python trae `unittest`. Agrupas las pruebas en una clase y usas métodos
como `assertEqual`. Ejecuta esto y mira el informe:

<PyExercise
  task="Ejecuta esta batería de pruebas con unittest y observa el informe. (Se ejecuta igual en tu máquina.)"
  starter={`import unittest

def suma(a, b):
    return a + b

class TestSuma(unittest.TestCase):
    def test_positivos(self):
        self.assertEqual(suma(2, 3), 5)
    def test_cero(self):
        self.assertEqual(suma(0, 0), 0)

unittest.main(argv=["", "-v"], exit=False)`}
/>

<Pitfall>
Prueba también los **casos límite**, no solo el camino feliz: la lista vacía, el 0, un número
negativo, una entrada inesperada. Ahí es donde se esconden la mayoría de los errores.
</Pitfall>

## Mini-ejercicios

<Collapsible title="1. ¿Qué es TDD (desarrollo guiado por pruebas)?">

Escribir la **prueba primero** (que falla porque aún no hay código), luego el código mínimo para que
pase, y después limpiar. El ciclo *rojo → verde → refactor*. Ayuda a diseñar y a no romper lo que ya
funciona.

</Collapsible>

<Collapsible title="2. En proyectos reales, ¿unittest o pytest?">

Ambos valen. `unittest` viene incluido; `pytest` (se instala aparte con `pip install pytest`) es más
conciso: te deja usar `assert` normal y descubre las pruebas solo. Muchos proyectos usan `pytest`.

</Collapsible>

---

¡Módulo 5 completado — ya escribes Python avanzado! Es hora del **Proyecto 8**: un **motor de aventura
de texto**, donde juntarás clases, estado y análisis de comandos en un mundo explorable.
